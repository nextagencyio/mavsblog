---
import { Image } from 'astro:assets';
import FormattedDate from './FormattedDate.astro';
import CategoryBadge from './CategoryBadge.astro';
import type { ImageMetadata } from 'astro';

interface Props {
	title: string;
	description: string;
	pubDate: Date;
	slug: string;
	tags?: string[];
	variant?: 'horizontal' | 'vertical';
	heroImage?: ImageMetadata;
}

const { title, description, pubDate, slug, tags = [], variant = 'horizontal', heroImage } = Astro.props;
const displayTag = tags.find(t => t !== 'featured') || tags[0];
---

<a href={`/blog/${slug}/`} class={`article-card ${variant}`}>
	<div class="card-thumb">
		{heroImage ? (
			<Image src={heroImage} alt={title} class="thumb-img" width={variant === 'horizontal' ? 400 : 600} height={variant === 'horizontal' ? 300 : 320} />
		) : (
			<div class="thumb-placeholder">
				<img src="/images/mavsboard-logo.png" alt="" class="thumb-logo" />
			</div>
		)}
	</div>
	<div class="card-body">
		{displayTag && <CategoryBadge tag={displayTag} />}
		<h3 class="card-title">{title}</h3>
		<p class="card-desc">{description}</p>
		<span class="card-date"><FormattedDate date={pubDate} /></span>
	</div>
</a>

<style>
	.article-card {
		display: flex;
		background: var(--card-bg);
		border-radius: var(--radius);
		overflow: hidden;
		text-decoration: none;
		box-shadow: var(--box-shadow);
		transition: transform 0.2s, box-shadow 0.2s;
	}
	.article-card:hover {
		transform: translateY(-2px);
		box-shadow: var(--box-shadow-hover);
		text-decoration: none;
	}

	.article-card.horizontal {
		flex-direction: row;
	}
	.horizontal .card-thumb {
		flex: 0 0 240px;
		min-height: 160px;
	}
	.horizontal .card-body {
		flex: 1;
		padding: 1.25rem;
		display: flex;
		flex-direction: column;
		justify-content: center;
	}

	.article-card.vertical {
		flex-direction: column;
	}
	.vertical .card-thumb {
		height: 180px;
	}
	.vertical .card-body {
		padding: 1rem;
	}

	.card-thumb {
		position: relative;
		overflow: hidden;
	}
	.thumb-img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		border-radius: 0;
	}
	.thumb-placeholder {
		width: 100%;
		height: 100%;
		background: linear-gradient(135deg, #0b1a2e, #0053BC);
		display: flex;
		align-items: center;
		justify-content: center;
	}
	.thumb-logo {
		width: 50px;
		height: auto;
		opacity: 0.3;
	}
	.card-title {
		font-size: 1.05rem;
		margin: 0.35rem 0;
		color: var(--text);
		line-height: 1.3;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}
	.card-desc {
		font-size: 0.85rem;
		color: var(--text-muted);
		margin: 0.25rem 0;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
		line-height: 1.5;
	}
	.card-date {
		font-size: 0.78rem;
		color: var(--text-muted);
		margin-top: auto;
	}

	@media (max-width: 768px) {
		.article-card.horizontal {
			flex-direction: column;
		}
		.horizontal .card-thumb {
			flex: none;
			height: 180px;
		}
	}
</style>
